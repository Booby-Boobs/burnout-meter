{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "windows-safe",
  "description": "Capabilities safe for Windows",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default",
    "shell:allow-open",
    "dialog:default"
  ]
}
